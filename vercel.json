{
  "buildCommand": "cd azure-deploy && npm install && npm run build",
  "outputDirectory": "azure-deploy/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}